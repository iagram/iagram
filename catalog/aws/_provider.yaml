# Terraform provider configuration for every aws.* entry.
name: aws
source: hashicorp/aws
version: "~> 6.0"
required_version: ">= 1.6.0"
extra_providers:
  archive: {source: hashicorp/archive, version: "~> 2.4"}
