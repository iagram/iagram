id: common.user
label: User
description: A person using the system.
category: general
icon: common/user.svg
kind: leaf
allowed_parents: ["*"]
size: {w: 100, h: 90}
props:
  type: object
  properties:
    caption:
      type: string
      title: Caption
      description: Short role shown under the name (for example "OEM engineer").
