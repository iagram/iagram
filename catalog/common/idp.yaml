id: common.idp
label: Identity provider
description: External OAuth / SAML identity provider.
category: general
icon: common/saml_token.svg
kind: leaf
allowed_parents: ["*"]
size: {w: 100, h: 90}
props:
  type: object
  properties:
    caption:
      type: string
      title: Caption
      description: Short role shown under the name (for example "OEM engineer").
