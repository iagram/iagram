id: common.datacenter
label: Corporate data center
description: On-premises site, branch or partner network. Holds the customer side of hybrid links (customer gateways, on-prem servers, databases).
category: annotation
icon: common/office_building.svg
kind: container
transparent: true
allowed_parents: ["*"]
size: {w: 420, h: 280}
style: {border: "#7D8998", dash: solid, label: left}
props:
  type: object
  properties:
    cidr:
      type: string
      title: CIDR block
      format: cidr
      description: Address range of the site, shown in the label.
