id: common.note
label: Note
description: Free text placed anywhere on the diagram (configuration details, assumptions, legend).
category: annotation
icon: common/document.svg
kind: leaf
allowed_parents: ["*"]
size: {w: 200, h: 100}
props:
  type: object
  properties:
    text:
      type: string
      title: Text
      format: multiline
      default: ""
