# ee/

Source-available code under the [Elastic License 2.0](LICENSE). Nothing lives here yet; the directory exists so the licensing boundary is explicit from day one. Everything the core tool does (drawing, validating, generating, planning, applying, every cloud provider and every catalog element) stays under Apache-2.0 outside this directory.
